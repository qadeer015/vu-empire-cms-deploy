<div class="container p-2 d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 mb-0">
        <a href="/" class="hover-underline rounded-pill fs-4">
            <img src="/assets/brand/<%= user && user.id == 1 ? 'genie' : 'logo' %>.png" width="80" alt="VU Empire <%= user && user.id==1 ? 'Genie' : '' %>">
        </a>
    </h1>
    <div>
        <!-- TODO: add login/logout buttons here -->
    </div>
</div>