<!-- assignments/_form.ejs -->
<div class="container-fluid">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 mb-0"><%= mode === 'new' ? 'Add Assignment' : 'Edit Assignment' %></h1>
  </div>
  <%- include('../partials/_breadcrumb') %>

  <div class="card border-0 shadow-sm">
    <div class="card-body">
      <% if (mode === 'edit' && assignment) { %>
      <form action="/assignments/<%= assignment.id %>" method="POST">
        <% } else { %>
        <form action="/assignments" method="POST">
          <% } %>

          <div class="row g-3">
            <div class="col-md-6">
              <label class="form-label fw-bold">Course <span class="text-danger">*</span></label>
              <select name="courseId" class="form-select" required>
                <option value="">Select Course</option>
                <% courses.forEach(function(c) { %>
                <option value="<%= c.courseId %>" <%= (assignment && assignment.courseId == c.courseId) || (!assignment && selectedCourseId && selectedCourseId == c.courseId) ? 'selected' : '' %>>
                  <%= c.courseCode %> - <%= c.courseName %>
                </option>
                <% }); %>
              </select>
            </div>
            <div class="col-md-6 d-flex align-items-end text-muted small pb-2">
              <span><i class="bi bi-info-circle me-1"></i>Course code and name are managed in the Courses section.</span>
            </div>
            <div class="col-12">
              <label class="form-label fw-bold">Assignment Title <span class="text-danger">*</span></label>
              <input type="text" class="form-control" name="title" value="<%= assignment ? assignment.title : '' %>" required>
            </div>
            <div class="col-12">
              <label class="form-label fw-bold">Description</label>
              <textarea class="form-control" name="description" rows="3" placeholder="Assignment description or instructions..."><%= assignment ? assignment.description : '' %></textarea>
            </div>
            <div class="col-md-6">
              <label class="form-label fw-bold">Due Date</label>
              <input type="datetime-local" class="form-control" name="dueDate" value="<%= assignment && assignment.dueDate ? new Date(assignment.dueDate).toISOString().slice(0, 16) : '' %>">
            </div>
            <div class="col-md-6">
              <label class="form-label fw-bold">Status</label>
              <select name="status" class="form-select">
                <option value="draft" <%= assignment && assignment.status === 'draft' ? 'selected' : '' %>>Draft</option>
                <option value="publish" <%= assignment && assignment.status === 'publish' ? 'selected' : '' %>>Published</option>
                <option value="pending" <%= assignment && assignment.status === 'pending' ? 'selected' : '' %>>Pending</option>
              </select>
            </div>
            <div class="col-md-6">
              <label class="form-label fw-bold">File Path / URL</label>
              <input type="text" class="form-control" name="filePath" value="<%= assignment && assignment.filePath ? assignment.filePath : '' %>" placeholder="URL to assignment file">
            </div>
            <div class="col-md-6">
              <label class="form-label fw-bold">Original Filename</label>
              <input type="text" class="form-control" name="originalFilename" value="<%= assignment && assignment.originalFilename ? assignment.originalFilename : '' %>" placeholder="e.g. CS201-Assignment1.pdf">
            </div>
          </div>

          <div class="mt-4 d-flex gap-2">
            <button type="submit" class="btn btn-primary">
              <i class="bi bi-check-lg"></i> <%= mode === 'new' ? 'Create Assignment' : 'Update Assignment' %>
            </button>
            <a href="/assignments" class="btn btn-outline-secondary">Cancel</a>
          </div>
        </form>
    </div>
  </div>
</div>